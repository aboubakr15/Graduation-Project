[build]
  builder = "nixpacks"

[deploy]
  startCommand = "gunicorn GraduationProject.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120"
  preDeployCommand = "python manage.py migrate"

