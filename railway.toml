[build]
  builder = "nixpacks"

[deploy]
  # daphne serves both HTTP and WebSocket via the ASGI application
  startCommand = "daphne -b 0.0.0.0 -p $PORT GraduationProject.asgi:application"
  preDeployCommand = "python manage.py migrate"

